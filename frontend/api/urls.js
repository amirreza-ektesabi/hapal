const hostUrl = "http://localhost:8000";

export const user = `${hostUrl}/account/{0}/`;
export const list = `${hostUrl}/list/{0}/`;
export const lists = `${hostUrl}/{0}/{1}/lists/`;
export const post = `${hostUrl}/post/{0}/`;
export const posts = `${hostUrl}/{0}/{1}/posts/`;
export const comments = `${hostUrl}/{0}/{1}/comments/`;
export const properties = `${hostUrl}/post/{0}/properties/`;
